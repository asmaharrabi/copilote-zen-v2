# Supabase (Postgres + pgvector + Realtime)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=


# IA générative (Groq — génération) + OpenAI (embeddings uniquement)
GROQ_API_KEY=
GOOGLE_API_KEY=
# Email transactionnel (notifications de ticket)
RESEND_API_KEY=

# Notification Slack sur escalade
SLACK_WEBHOOK_URL=
# Secret partagé pour valider les webhooks entrants (n8n -> app)
WEBHOOK_SHARED_SECRET=

# URL publique de l'app (utilisée par n8n et les liens dans les emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000
